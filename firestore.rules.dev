rules_version = '2';

// REGRAS DE DESENVOLVIMENTO (totalmente abertas — só para emulador)
// Use firestore.rules.production para produção
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
