import React from 'react';
import { AppShell } from './src/AppShell';

/** Entry point native (Android/iOS). Veja App.web.tsx pra versao web. */
export default function App() {
  return <AppShell />;
}
